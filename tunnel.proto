syntax = "proto3";

package tunnel;

message Req {
    enum Type { ZERO = 0; TYPE = 1; }
    uint32 id = 1;
    string host = 2;
    string port = 3;
}

message Resp {
    enum Type { ZERO = 0; TYPE = 2; }
    uint32 id = 1;
    uint32 host = 2;
    uint32 port = 3;
}

message Data {
    enum Type { ZERO = 0; TYPE = 3; }
    bytes data = 1;
}
