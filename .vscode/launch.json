{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "linux",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/local/local",
            "cwd": "${workspaceFolder}",
        }
    ]
}