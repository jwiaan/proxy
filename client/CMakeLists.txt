cmake_minimum_required(VERSION 3.10)
project(client)
add_compile_options(-Wall -g3)
add_subdirectory(.. proxy)
aux_source_directory(. client)
add_executable(${PROJECT_NAME} ${client})
target_link_libraries(${PROJECT_NAME} proxy ssl protobuf)
